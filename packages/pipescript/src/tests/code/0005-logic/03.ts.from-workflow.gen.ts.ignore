function file_ts() {
    function file_ts_if_body(condition: bool, b: number, a: number) {
        function file_ts_if_body_if_body(condition: bool, b: number, a: number) {
            const b = 46;
            const a = 50;
            return { b, a };
        }
        const b_5 = 43;
        const value = a > b_5;
        const { b, a } = file_ts_if_body_if_body(value, b_5, a);
        return { b, a };
    }
    const a_1 = 42;
    const b_2 = undefined;
    const value = a_1 > 40;
    const { b, a } = file_ts_if_body(value, b_2, a_1);
    return { a, b };
}