function file_ts() {
    function file_ts_if_body(condition: bool, b: number, a: number) {
        function file_ts_if_body_if_body(condition: bool, b: number) {
            const b_8 = 46;
            const b = condition ? b_8 : b;
            return b_7;
        }
        const b_5 = 43;
        const value = a > b_5;
        const b_7 = file_ts_if_body_if_body(value, b_5);
        const b = condition ? b_7 : b;
        return b;
    }
    const a = 42;
    const b_2 = undefined;
    const value = a > 40;
    const b = file_ts_if_body(value, b_2, a);
    return b;
}