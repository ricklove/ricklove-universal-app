const file.ts/if-body = (condition: bool, b: number, a: number) => {
const file.ts/if-body/b= = (old: number, value: number) => {

}

const file.ts/if-body/> = (left: number, right: number) => {

}

const file.ts/if-body/if-body = (condition: bool, b: number) => {
const file.ts/if-body/if-body/b= = (old: number, value: number) => {

}
}
}
const file_ts_1_a = 42;

export const b = undefined;

const value = file.ts/>(left, right);

const { b, file_ts_4_a } = file.ts/if-body(condition, b, a);