function file_ts() {
    function file_ts_if_body(condition: bool, b: number, a: number) {
        function file_ts_if_body_if_body(condition: bool, b: number) {
            const b = old;
            return b_7;
        }
        const b = old;
        const value = left > b;
        const b_7 = file_ts_if_body_if_body(value, b);
        return { b_4, a_4 };
    }
    const a = 42;
    const b_2 = undefined;
    const value = a > 40;
    const { b_4, a_4 } = file_ts_if_body(value, b_2, a);
    return b;
}