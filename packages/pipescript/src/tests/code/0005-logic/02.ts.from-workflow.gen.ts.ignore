function file_ts() {
    function file_ts_if_body(condition: bool, b: number, a: number) {
        function file_ts_if_body_if_body(condition: bool, b: number) {
            const b = 46;
            return b;
        }
        const b_5 = 43;
        const value = left > b_5;
        const b = file_ts_if_body_if_body(value, b_5);
        return { b, a };
    }
    const a_1 = 42;
    const b_2 = undefined;
    const value = a_1 > 40;
    const { b, a } = file_ts_if_body(value, b_2, a_1);
    return b;
}