function file_ts() {
    function file_ts_if_body(condition: bool, b: number) {
        const b = old;
        return { b };
    }
    const a = 42;
    const b = undefined;
    const value = a > 40;
    const b = file_ts_if_body(value, b);
    return { b };
}