function file_ts() {
    function file_ts_if_body(condition: bool, b: number) {
        const b = old;
        return b_4;
    }
    const a = 42;
    const b = undefined;
    const value = a > 40;
    const b_4 = file_ts_if_body(value, b);
    return b;
}