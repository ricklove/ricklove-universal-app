const file.ts/* = (left: number, right: number) => {

}

const file.ts/- = (left: number, right: number) => {

}

const file.ts/* = (left: number, right: number) => {

}

const file.ts/+ = (left: number, right: number) => {

}

const file.ts/+ = (left: number, right: number) => {

}
const a = 42;

const file.ts:3 = file.ts/*(left, right);

const file.ts:4 = file.ts/-(left, right);

const file.ts:5 = file.ts/*(left, right);

const file.ts:6 = file.ts/+(left, right);

const file.ts:7 = file.ts/+(left, right);

export const b = value;